import {Card, CardContent} from "@mui/material";

const ProfileDropdown = () => {
    return (
        <Card>
            <CardContent>

            </CardContent>
        </Card>
    )
}

export default ProfileDropdown;
