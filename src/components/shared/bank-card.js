import {Card, CardContent} from "@mui/material";

const BankCard = ({card, color}) => {
    return (
        <Card elevation={0}>
            <CardContent>

            </CardContent>
        </Card>
    )
}
